// Translation subsystem public interface

// Services (used by other subsystems)
const translationService = require('./services/translationService');

module.exports = {
  // Services
  translationService
};
